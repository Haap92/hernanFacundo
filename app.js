//Aca se va a ejecutar la aplicacion.
let funciones = require("./funciones")
function elMasGrande (numeroA, numeroB){
if(numeroA>numeroB){
    return (numeroA + " es mas grande que " + NumeroB)
}
else {
    return (numeroB + " es mas grande que " + numeroA)
}
}
